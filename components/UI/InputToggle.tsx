import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {
  text: string;
};

const InputToggle = ({ text }: Props) => {
  return (
    <View>
      <Text>InputToggle</Text>
    </View>
  );
};

export default InputToggle;

const styles = StyleSheet.create({});
